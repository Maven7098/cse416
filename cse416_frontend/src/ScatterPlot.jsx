// Code from https://www.react-graph-gallery.com/scatter-plot
import * as d3 from "d3"; // we will need d3.js

export const Scatterplot = ({ data }) => {
    const width = 960; const height = 360;

    // read the data
    // do some stuff with d3
    // compute all the <circle>

  return (
    <div>
      <svg width={width} height={height}>
        {/* render all the <circle> */}
      </svg>
    </div>
  );
};